# Exp 23: 2 deterministic pursuers, 5x5, multi-layout training.
# Tests whether doubled adversarial pressure changes the OOD benefit.
# State space: 25 x 25 x 4 = 2,500 (nearest pursuer encoded). Multi-layout (10 layouts).

project: {}

run:
  debug:
    train_episodes: 40000

training:
  num_train_layouts: 10

rewards:
  distance_shaping_enabled: true
  distance_shaping_scale: 1.0

eval:
  ood_layout_count: 10

experiment: {}
scenarios:
  adversary:
    adversary_count: 2
