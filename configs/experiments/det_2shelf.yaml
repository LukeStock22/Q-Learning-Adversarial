# Exp 22: 2 static shelves + deterministic adversary, 5x5, multi-layout training.
# Tests whether denser obstacles eliminate the OOD advantage.
# State space: 25 x 25 x 4 = 2,500 (shelves not encoded). Multi-layout (10 layouts).

project: {}

run:
  debug:
    train_episodes: 40000

training:
  shelf_count: 2
  num_train_layouts: 10

rewards:
  distance_shaping_enabled: true
  distance_shaping_scale: 1.0

eval:
  ood_layout_count: 10

experiment: {}
scenarios: {}
