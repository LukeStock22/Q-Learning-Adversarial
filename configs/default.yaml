project:
  name: qlearning-adversarial
  grid_size: 10
  seed: 7
  output_dir: outputs
training:
  episodes: 1000
  max_steps: 200
  alpha: 0.1
  gamma: 0.95
  epsilon: 0.1
eval:
  episodes: 50
  max_steps: 200
